stages:
  - test
  - build

test backend:
  stage: test
  image: golang:1.11
  tags:
    - docker
  script:
    - ./pipelines.sh test backend
  cache:
    untracked: false
    key: backend
    paths:
      - backend/vendor
    policy: pull-push

build backend:
  stage: build
  image: docker:stable
  tags:
    - docker
  services:
    - docker:dind
  script:
    - ./pipelines.sh build backend
  cache:
    untracked: false
    key: backend
    paths:
      - backend/vendor
    policy: pull
