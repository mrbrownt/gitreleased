stages:
  - test
  - build

test backend:
  stage: test
  image: golang:1.11
  script:
    - ./pipelines.sh test backend
  cache:
    untracked: false
    key: backend
    paths:
      - backend/vendor
    policy: pull-push

build backend:
  stage: build
  image: docker:stable
  services:
    - docker:dind
  script:
    - ./pipelines.sh build backend
  cache:
    untracked: false
    key: backend
    paths:
      - backend/vendor
    policy: pull
